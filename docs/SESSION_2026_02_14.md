# Session 2026-02-14: Game/Universe Merge & Create Game

## Changes

- Merged `GameConfig` + `UniverseConfig` into single flat `GameConfig`
- Game names auto-generated (8-char hex via `crypto/rand`)
- Added seeded deterministic universe generation — hex string seed stored on game, parsed to int64 for RNG
- Seed optional in API (auto-generated if omitted, can be provided to reproduce a universe)
- Config `UniverseConfig` renamed to `GameConfig`, wired as default source for handler
- Consolidated 3 migration files into single `001_initialize_schema.sql`
- Updated `.env`, `.env.example`, `README.md`, `CLAUDE.md`

## Frontend (planets-admin)

- Extended `fetchAPI` with optional `options` param for POST
- Added `createGame` API + `useCreateGame` mutation hook
- Built Create New Game dialog with all-optional config fields (seed, max_players, turn_interval_hours, galaxy/sector/system/planet counts)
- Installed shadcn `dialog`, `input`, `label`

## Key Decisions

- Seed is a hex string (not int64) to avoid JS precision loss
- `name` removed from `GameConfig` — never user-provided
- Universe reuse strategy: same seed + same config = identical universe (no need to clone DB rows)
- Single migration file during early development — no data to preserve yet
